import { Login } from "@/components/login";
import React from "react";

type Props = {};

const page = (props: Props) => {
  return (
    <div>
      <Login />
    </div>
  );
};

export default page;
